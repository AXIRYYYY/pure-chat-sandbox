# 更新日志 (Changelog)

所有对此项目的重大更改都将记录在此文件中。

## [5.4.3] - 2026-09-12

### 新增
- **`.gitattributes` 换行规范**: `*.bat` 强制 CRLF（cmd.exe 必需），文本文件统一 `text=auto` 入库 LF；根治中文批处理文件被 cmd 误读为乱码命令的问题。

### 修复
- **启动沙盒.bat 乱码报错**: 纯 LF 换行导致 `echo 中文` 被 cmd 读串行报“不是内部或外部命令”，转 CRLF 后修复；`update_tools.bat` 同步修复。
- **Gem 工作区刷新死循环**: 恢复 `gem_instruction` 保存三行的 20 格缩进（误置 12 格导致每轮 `st.rerun()`，压缩/索引/联网模块消失）。
- **api_config.json 被默认骨架覆盖**: 从备份恢复 9 个工作区与全部 Key/prefs，保留 `api_config.json.overwritten_20260912` 现场。

### 优化
- **侧边栏布局**: 上下文压缩折叠为外层 expander（无区间折叠、有区间自动展开），与对话索引一起前移至联网开关之前；调整分隔线顺序。

## [5.4.2] - 2026-09-12

### 新增
- **OpenCode Go 供应商预设**: `provider_registry.py` 新增 `opencode-go` 条目（`https://opencode.ai/zen/go/v1`，独立 Key 位），与 Zen 同协议、地址与 Key 不同；模型拉取走通用 `/models` 探测，零额外改动。

### 优化
- **README 特性区开头新增“与常见对话页的区别”**: 点名中间段多区间可逆压缩、对比 Tabs 复用搜索缓存、供应商与模型全动态、搜索结果独立缓存、缓存前缀稳定保障 5 项差异。
- **压缩措辞强化**: 明确“任意连续区间（含中间段）”与“多区间并存（如 2-4 轮与 6-8 轮）”。
- **核心特性分组详述**: 从 10 条一句话扩为 7 组 27 条，覆盖压缩/对比/多供应商/审计等此前零覆盖功能；修正 4 处过时表述；streamlit 徽标与 requirements 对齐。
- **热门供应商卡片折叠**: 开关关闭的供应商折叠收起，启用的展开，状态按注册表每轮重算。
- **Key 设置去重**: 移除顶部与供应商卡片重复的 Gemini/SiliconFlow/DeepSeek 输入框，顶部仅保留 Tavily/博查并改名为“联网搜索 Key 设置”，统一保存按钮不变。

### 修复
- **删除模板孤儿字段**: `api_config.example.json` 移除零代码读取的 `ds_free_api_key` 与 `cache_ttl`。
- **`config_manager.py` 默认值补齐**: 新增 `search_engine` / `reasoning_effort` / `web_search_extract_config` / `compression_defaults`，取值与代码 `.get()` 兜底一致。
- **提取模型帮助文案**: 不再写死 Tavily，改为“当前选择的搜索引擎”。

## [5.4.1] - 2026-09-07

### 开源发布准备
- **版本号统一**: `app.py`、`README` 徽标统一为 5.4.1；修正 5.4.0 条目日期。
- **公开文件集**: 取消跟踪 `model_config.json`、`AGENTS.md`、`converter.py`，不预制模型清单、不公开内部指南与一次性脚本。
- **配置模板补全**: `api_config.example.json` 新增 `bocha` 与 `compression_defaults` 字段。
- **README 修订**: 修正启动/获取模型步骤顺序；多供应商措辞；补全项目结构树；`copy` 补充 `cp` 说明。
- **LICENSE 署名**: `Copyright (c) 2026 AXIRYYYY`。

## [5.4.0] - 2026-07-22

### 新增
- **非侵入式上下文压缩 (Context Compression)**:
  - 新增 `compression_engine.py` 模块，提供完整的上下文压缩引擎。
  - 支持自定义压缩提示词：用户可编辑指导压缩模型如何总结对话。
  - 支持自由区间选择：以对话轮次为单位，选择任意连续区间执行压缩。
  - 多区间压缩：可同时对多个不重叠区间分别压缩（如压缩 2-4 轮 + 6-8 轮）。
  - 区间重叠防护：自动检测已有压缩区间，防止重叠压缩。
  - 可逆操作：原始消息完整保留（messages 数组不受影响），取消压缩瞬间恢复。
  - 压缩模型独立选择：可选择任意可用模型通道/模型执行压缩，与主对话模型解耦。
  - 配置持久化：压缩模型、提示词可保存到 `api_config.json`。
  - Gemini 和 OpenAI 兼容路径双路径适配：context 组装时自动替换压缩区间为摘要消息。
  - 搜索缓存兼容：OpenAI 路径下的搜索结果注入与压缩逻辑透明协作。
- **对话索引 (Conversation Index)**:
  - 侧边栏新增「📋 对话索引」面板，以轮次列表展示所有对话。
  - 每轮显示用户提问截断预览 + 可点击跳转至对应消息位置。
  - 悬停显示完整首句（HTML title），点击通过锚点平滑滚动。
  - 零 CSS 注入：全部使用 Streamlit 原生组件实现。
  - 纯 HTML 锚点跳转，不触发 Streamlit 重跑。
- **工作区切换时清理压缩状态**: 切换工作区时自动重置压缩状态，避免跨区污染。

### 修复
- **删除/重置对话时压缩状态自动清理**: `delete_turn`、Gem 重置、会话销毁入口均同步调用 `cleanup_deleted_messages()`，确保无孤儿压缩区间残留。

## [5.3.2] - 2026-07-20

### 新增
- **分支对比生成 (Branch Comparison)**: 在任意 AI 回复下方展开「⚖️ 对比其他模型生成」，选择不同平台/模型生成对比结果，以 `st.tabs` 形式并排展示，便于审计级模型比对。
- **确定性 JSON 序列化**: 所有对话保存强制 `json.dumps(sort_keys=True)`，确保相同数据产生相同字节流，保障 Gemini Context Cache 100% 命中率。
- **自动备份机制**: `save_chat_history()` 首次写入对话文件前生成 `.bak` 备份，防止意外数据丢失。
- **`chat_utils.py` 新增工具函数**: `deterministic_json_dumps()`、`compute_payload_hash()`、`save_chat_history()`、`normalize_messages()`、`msg_has_branches()`。

### 修复
- **分支对比时附件内容丢失**: `trigger_compare` 从独立 `if` 块移入 `if/elif/else` 链，阻止执行流穿透到 `else:` 分支导致 `full_payload = prompt` 覆盖已正确提取的附件内容。

### 技术改进
- `app.py` 消息渲染循环支持多分支 Tabs 展示；所有保存均改为调用 `save_chat_history()` 统一入口。
- **多供应商通道架构**: 6 文件修改（`app.py`、`ai_models_fetcher.py`、`token_utils.py`、`config_manager.py`、`pages/model_management.py`），1 新文件（`provider_registry.py`），代码净增 ~450 行。

### 修复
- **模型通道动态化**: App 侧边栏通道选择从硬编码 `st.radio(["Gemini", "SiliconFlow", "DeepSeek"])` 改为动态 `st.selectbox` — 仅显示已填写 API Key 且有可用模型的通道，未配置的供应商不显示。
- **后端 OpenAI 兼容路径统一**: SiliconFlow 和 DeepSeek 的执行分支合并为通用 OpenAI 兼容路径，通过供应商注册表路由 api_host 和 api_key，消除 200+ 行重复代码。
- **API Key 统一保存**: 热门供应商区和自定义接口区的 Key 输入不再自动保存（避免各区域独立写盘的混乱），改为由单一「💾 保存所有 API Key 与供应商配置」按钮收集全页 Key 统一落盘。
- **模型获取 HTTP 层修复**: `ai_models_fetcher.py` 从 Python 原生 `urllib` 替换为 `requests` 库，解决 `urllib` 的 TLS 指纹被 Cloudflare Bot Fight Mode（Error 1010）拦截的问题（如 OpenCode Zen API）；新增浏览器 User-Agent 头与 `Accept: application/json` 头。
- **Chat 连通性验证**: `/models` 端点获取失败时自动发起最小 chat 请求验证 Key 有效性，区分「Key 无效」与「/models 端点不可用」两种场景。
- **`token_utils.py` 通用化**: 平台判断从硬编码名称改为 `is_gemini_type()`/`is_openai_type()` 函数。
- **模型获取报告 UI**: 「获取最新模型列表」按钮返回**逐供应商结果报告**，清晰展示每个供应商的获取状态、模型数及错误详情（替代原有的静默终端 print）。

### 新增
- **多供应商通道架构**:
  - 新增 `provider_registry.py` 供应商注册表核心模块，内置 16 个热门 LLM 供应商预设（Google Gemini、DeepSeek、SiliconFlow、OpenAI、阿里百炼、智谱AI、月之暗面、MiniMax、Groq、OpenRouter、xAI、Ollama、火山引擎、阶跃星辰、Together AI、Mistral AI），并支持 OpenCode Zen。
  - 新增 `provider_registry.json` 运行时供应商配置文件，存储启用状态与自定义接口定义。
  - 新增无限自定义 OpenAI 兼容接口机制：用户可添加任意遵循 `/v1/chat/completions` 格式的 API 供应商（名称、API Host、Key），动态增删。
- **模型管理页 UI 重构**:
  - 「热门供应商」卡片区：网格布局展示 16 个供应商预设，支持一键启用 + API Key 配置。
  - 「自定义 OpenAI 兼容接口」区：支持无限添加/编辑/删除自定义供应商。

### 开源准备
- **MIT 许可证**: 正式采用 MIT License 开源。
- **Git 历史清洗**:
  - 使用 `git filter-repo` 从所有分支历史移除 `api_config.json`、`chat_logs/`、`attachments_cache/`，不影响本地副本。
  - 移除误提交的 7 个 `__pycache__/*.pyc` 字节码缓存文件、空 `.gitmodules`、过期的快捷方式文件。
- **白名单 `.gitignore`**: 从黑名单模式改为白名单模式，默认屏蔽一切，仅追踪 17 个必要文件，从根本上杜绝敏感文件误提交。
- **`api_config.example.json`**: 新增 API 配置模板，所有密钥字段替换为占位符、workspace 标题与 system_prompt 匿名化，方便新用户快速上手。
- **移除预制模型清单**: `model_config.json` 不再内置模型列表，首次使用须在「模型库管理」页面获取最新模型后交叉选择。
- **README 全面修订**: 补充 UI 填写 / 编辑 JSON 文件双路径 API Key 配置指南；补上博查 API Key 获取地址，硅基流动条目排至表尾；模型管理从"可选"调整为"首次使用必需"，调至配置 API Key 之后；修正核心特性中的搜索描述（`Google 搜索` → `Tavily / 博查双引擎`）。
- **代码修正** (`app.py`): 2 处 API Key 缺失警告文案从"侧边栏"修正为指向「模型库管理」页面。
- **不再跟踪的文件**: `UI_FIX_REPORT.md`、`model_config.json` 取消 git 跟踪。

## [5.3.1] - 2026-07-17
### 修复
- **移除 >5MB 文件跳过解析限制**: Gem 知识库上传、编辑重发、首次发送附件时，不再因文件大小跳过文本解析，所有附件一律提取文字内容。

### 新增
- **PDF 文本增强清理**: 新增 `clean_pdf_text()` 函数，过滤零宽字符、Unicode 控制符/特殊标记，消除 PDF 文本中的编码污染。
- **PDF 解析容错增强**:
  - `process_uploaded_file()` 增加 PDF 魔数 (`%PDF`) 头部验证，防止伪 PDF 文件导致异常。
  - 解析失败时自动回退为纯文本读取，尽最大可能提取内容。
  - `read_pdf_content()` 增加 `fitz.FileDataError` / `fitz.EmptyFileError` 专项捕获。
- **附件解析详情面板**: 上传附件后在输入区上方展示 `📋 附件解析详情`，包含每个文件的字符数、体积、类型。

### 优化
- **CSS**: 扩大附件上传区域文件列表显示高度（默认 ~3行 → ~10行），减少长列表滚动操作。
- **代码清理**: `get_file_content_summary()` 移除已废弃的 `>5MB` 分支逻辑。

## [5.3.0] - 2026-07-18
### 新增
- **博查(Bocha)搜索引擎支持**: 在侧边栏联网搜索开关下方新增搜索引擎选择器（Tavily / 博查），支持实时切换。
- **`perform_bocha_search()` 通用搜索函数**: 位于 `chat_utils.py`，接口签名与 `perform_web_search()` 完全一致，支持预定义关键词（AI 建议搜索）跳过 AI 提取步骤。
- **搜索引擎感知的 API Key 校验**: 发送消息前根据所选引擎（Tavily/博查）检查对应的 API Key 是否已配置，未配置时给出明确拦截提示。
- **博查 API Key 配置入口**: 在「模型库管理」页面新增 `博查 Key` 输入框，支持密码模式输入，配置持久化到 `api_config.json`。

### 依赖
- 新增 `requests>=2.31.0` 依赖，用于调用博查搜索 REST API。

## [5.2.3] - 2026-05-13
### 修复
- **手机浏览器刷新丢失提示词修复（Bug 2）**: 修复了提交提示词后 AI 长期无响应时，手机浏览器刷新导致已提交但未收到回复的提示词永久丢失的问题。
  - **磁盘持久化 pending_msg**: 在设置 `pending_msg`（内存 session）的同时，同步写入磁盘文件 `pending_msg_{工作区}.json`，确保即使手机浏览器清除 session cookie，消息也不会丢失。
  - **页面加载自动恢复**: 页面加载时检测磁盘 pending 文件，自动恢复为编辑草稿，用户可修改后重新发送。
  - **全链路文件清理**: 在 API 成功完成、用户取消编辑等关键路径上同步清理磁盘 pending 文件，不留孤儿文件。
- **直接重新生成保持原发送时间**: 修复了点击"🔁 直接重新生成"时，用户消息末尾的提交时间被错误更新为当前时间的问题。现在重新生成时会从原始消息中提取并复用原来的时间戳，仅编辑后重新生成仍正常更新为当前时间。

## [5.2.2] - 2026-05-08
### 修复
- **重新生成按钮联网逻辑修正**: 修复了点击“重新生成”或“编辑并重新生成”时，由于 `not is_rerun` 硬编码限制导致无法联网查询的问题。
  - **智能补位搜索**: 引入 `has_cached` 检查机制。如果用户在首轮未开启联网，重新生成时只要开启开关，系统会因检测到“无缓存”而自动补位触发联网搜索。
  - **防重复注入**: 确保仅在无缓存时触发搜索，完美兼容“AI建议搜索”产生的合并缓存，避免了搜索结果的重复注入与配额浪费。
### 优化
- **联网搜索关键词提取逻辑重构**:
  - **跨平台支持**: 修复了关键词提取模型必须与主对话模型同平台的限制。现在支持“主模型用 A 平台，提取模型用 B 平台”的混合模式，实现成本最优组合（如：主模型用 R1，提取模型用免费的 SiliconFlow 模型）。
  - **独立 Client 机制**: 重构了 `app.py` 中的联网搜索触发流程，系统会根据配置为提取模型自动创建对应的 API 客户端（支持 Gemini SDK 和 OpenAI 兼容协议），解决了跨平台调用时的认证错误。
  - **UI 联动重构**: 在“模型库管理”页面将关键词提取模型的选择改为“先选平台，再选模型”的二级联动菜单，并支持手动输入自定义模型 ID。
  - **兼容性增强**: `perform_web_search` 函数现在可以透明处理不同平台的 SDK 差异，统一输出标准的搜索关键词。

## [5.2.1] - 2026-05-08
### 修复
- **Gem 逻辑与 UI 修正**:
  - **工作区隔离**: 修正了 "Gem 固定要求 (Fixed Prompt)" 的生效范围，确保其仅在名称包含 `(Gem)` 的工作区中才会被并入 System Prompt，避免对普通沙盒产生干扰。
  - **防呆机制**: 优化了侧边栏 UI 逻辑，"💎 Gem 专属定制" 模块现在仅在 Gem 工作区可见，防止在非 Gem 环境下产生误操作。
- **Gem 输入逻辑修正**: 
  - 将 "Gem 固定要求 (Fixed Prompt)" 从用户消息开头移至 System Prompt（系统指令）中，确保符合 Gem 原始设计逻辑。
  - 优化了 `get_effective_system_prompt()` 函数，使其支持合并全局指令、Gem 固定要求及联网搜索建议，并在 Gemini、SiliconFlow、DeepSeek 三大通道中同步生效。
  - 确保 Gem 固定要求在每一轮对话及缓存创建（Context Cache）时都能持续作为底层约束。
  - 修正了 Gem 实例化时的初始化文案，使其更符合当前的注入逻辑。

## [5.2.0] - 2026-05-07
### 新增
- **API 流式自动重连机制**:
  - 新增 `retry_api_stream()` 生成器函数，将流创建和逐块读取都包裹在重试逻辑内，实现流生命周期的完整重试保护。
  - Gemini、SiliconFlow、DeepSeek 三个平台的流式 API 调用全部改用新函数，替换原有的 `retry_api_call` + 裸迭代模式。
  - 修复了此前 503 错误在"逐块读取数据"阶段无法自动重连、直接进入草稿模式的问题。
- **可配置 API 自动重试次数**:
  - 在 `config_manager.py` 中添加默认配置（3 次），支持 -1（无限制重试）。
  - 在模型管理页面新增"API 自动重连设置"UI，支持下拉选择重试次数（不重试/1次/2次/3次/5次/10次/无限制）。
  - 配置即时生效并持久化到 `api_config.json`，重启页面不丢失。
- **联网搜索建议开关**:
  - 在 `config_manager.py` 中添加 `web_search_suggestion_enabled` 默认配置项（默认关闭）。
  - 在侧边栏 System Prompt 区域新增 `🔍 开启联网搜索建议` toggle 开关，开启后透明显示即将追加的指令内容，确保用户完全知情。
  - 新增 `get_effective_system_prompt()` 辅助函数，根据开关状态在 Gemini、SiliconFlow、DeepSeek 三个平台的请求中自动追加联网搜索建议指令，且避免重复追加。

### 修复
- **流式 API 503 错误的完整捕获**: 此前 `retry_api_call` 仅保护"建立连接"阶段，流读取中途的 503 错误未被捕获。现在使用 `retry_api_stream` 覆盖流整个生命周期，中途断流会自动以指数退避策略重连。

## [5.1.0] - 2026-05-01
### 新增
- **搜索结果独立缓存架构（search_cache）**:
  - 引入 `search_cache` 字典（key=`msg_id`, value=`{question, searches[]}`），搜索结果不再嵌入 `full_payload`，实现数据流零污染。
  - 每条用户消息新增 `msg_id` 字段（`msg_时间戳`），作为缓存键和精准清理的依据。
  - 历史消息构建时从缓存动态注入搜索结果，替代原有的 `full_payload` 嵌入方式。
  - 缓存随 `messages` 一起持久化到 JSON 文件（包装对象格式 `{messages, search_cache}`），关闭页面重新打开后搜索结果不丢失。
  - 向后兼容旧格式 JSON（纯数组格式自动识别）。
- **半自动 AI 联网搜索**:
  - AI 可在回答中主动建议搜索关键词（格式 `🔍 搜索：xxx`），用户点击"同意搜索"按钮触发联网 + 重新生成。
  - 手动输入关键词搜索支持独立的"不删除旧回答，直接生成新回答"模式。
  - 路径 A（AI 建议 → 同意）：删除旧回答、合并新旧搜索结果、重新生成。
  - 路径 B（手动输入）：保留旧回答、生成独立的新回答。

### 修复
- **根治 `raw_payload` 被搜索结果污染的问题**: `user_entry` 的 `raw_payload` 改用 `prompt`（原始输入），不再引用可能含搜索结果的 `full_payload`。
- **Rerun 时不再重复联网搜索**: 追加 `and not is_rerun` 条件，避免每次重绘都触发搜索 API 调用。
- **Rerun 时搜索结果上下文保留**: 通过 `preserved_msg_id` 机制，在重绘后从缓存重新注入搜索结果，确保 AI 不丢失搜索上下文。
- **路径 A 搜索使用 AI 建议的关键词**: 调整 `full_payload`/`raw_payload` 读取优先级，确保 AI 建议的新关键词被正确使用。
- **删除对话时同步清理 search_cache**: `delete_turn` 中通过 `msg_id` 精准删除对应缓存条目。
- **重置到 Gem 初始态时清理孤儿缓存**: 同步清理已被删除消息的 `search_cache` 条目。

## [5.0.4] - 2026-04-30
### 新增
- **可配置联网搜索关键词提取模型**:
  - 新增 `web_search_model` 配置项，允许用户为 SiliconFlow 和 DeepSeek 平台分别指定独立的 AI 模型用于联网搜索的关键词提取。
  - 在 `config_manager.py` 中设置默认值：SiliconFlow→`Qwen/Qwen2.5-72B-Instruct`，DeepSeek→`deepseek-chat`。
  - 在 `pages/model_management.py`（模型库管理页面）新增 **🌐 联网搜索 - 关键词提取模型** 配置区，支持从可用模型列表选择或手动输入自定义模型 ID。

### 优化
- **联网搜索逻辑解耦**: `app.py` 中两处联网搜索调用（SiliconFlow / DeepSeek）不再硬绑定 `target_model`（主对话模型），改为读取 `api_configs["web_search_model"]` 中的专用配置，无配置时以 `target_model` 保底。
- **模型变更通知增强**: 在 `pages/model_management.py`（模型库管理页面）中，获取最新模型列表后，新增 **"发现模型减少"** 的黄色警告通知，同时展示新增和减少的模型，让用户对模型库变化一目了然。

### 修复
- **修复联网搜索因模型不兼容失败的问题**: 此前联网搜索的关键词提取使用的是用户选定的主对话模型（如 `deepseek-reasoner`），该模型在不加特殊 `thinking` 参数时返回空内容，导致 Tavily 报 "Query is missing"。现在使用独立配置的轻量模型进行关键词提取，彻底解决了该问题。

## [5.0.3] - 2026-04-29
### 优化
- **代码重构 - 联网搜索模块合并**:
  - 将 SiliconFlow 和 DeepSeek 平台中重复的联网搜索代码（关键词提取 + Tavily 搜索 + 结果格式化）提取为共用函数 `perform_web_search()`，存放于 `chat_utils.py`。
  - 两个平台的联网搜索代码量从 ~100 行（两处重复）缩减至 ~60 行（单一函数 + 两处调用），后续修改搜索逻辑只需改动一个地方，大幅降低维护成本。

## [5.0.2] - 2026-04-29
### 新增
- **动态模型获取系统**:
  - 新增 `ai_models_fetcher.py` 模块，支持从 Gemini、SiliconFlow、DeepSeek API 自动获取最新模型列表，实现零依赖模型发现。
  - 新增 `available_models.json` 存储所有可用模型，`enabled_models.json` 存储用户启用的模型。
- **模型库管理界面**:
  - 新增 `pages/model_management.py` 页面，提供图形化模型管理界面。
  - 支持一键获取最新模型、选择启用/禁用模型，提升模型配置灵活性。
- **模型配置优化**:
  - 更新 `model_config.json`，新增 DeepSeek 原生模型通道，调整 SiliconFlow 模型列表。
  - 支持自定义模型名称与动态模型加载。

### 优化
- **代码结构**: 进一步模块化，分离模型管理逻辑到独立页面。
- **用户体验**: 简化模型切换流程，提供更直观的模型选择界面。

## [5.0.1] - 2026-04-03
### 修复
- **UI 交互修复**:
  - 解决了关闭侧边栏后“打开侧边栏”按钮消失或无法点击的问题，恢复了 Streamlit 原生交互逻辑。
  - 修复了顶栏（Header）遮挡最右侧滚动条顶部的问题，通过将顶栏背景设为透明实现。
  - 优化了主内容区域的顶部边距（Padding），防止内容与顶栏按钮重叠。
- **文档补充**: 在工作区新增了 `UI_FIX_REPORT.md`，详细记录了本次 UI 问题的根因分析与解决方案。

## [5.0.0] - 2026-03-31
### 新增
- **模块化架构重构**: 
  - 将庞大的单体 `app.py` 拆分为职责明确的工具模块，大幅提升代码维护性：
    - `config_manager.py`: 集中管理 API 配置与模型参数。
    - `file_utils.py`: 统一 PDF、文本及 HTML 文件的深度解析逻辑。
    - `token_utils.py`: 实现多通道（Gemini/SiliconFlow）Token 的精确测算。
    - `chat_utils.py`: 抽象了工作区管理、反馈记录及对话轮次删除等核心流程。
- **对话智能命名**: 
  - 引入了基于 AI 意图分析的自动命名机制，在首轮对话后自动生成简洁案例标题。
  - 侧边栏新增“重新生成案例名称”按钮，支持根据上下文动态修正工作区标题。
  - 提供了独立的自动命名配置面板，支持自由切换命名模型与通道。
- **附件格式扩展**: 正式支持 `html` 格式文件的上传与解析，满足网页数据深度分析的需求。
- **Markdown 导出集成**: 通过 `export_utils.py` 实现了带元数据（附件标识、时间戳）的高质量 Markdown 资产导出。

### 优化
- **UI/UX 布局优化**:
  - **侧边栏结构化收纳**: 采用 `Expander` 嵌套结构将设置项分组显示，界面清爽专业。
  - **工作区可读性**: 工作区选择器升级，支持“案例标题 [工作区标识]”的增强显示模式。
- **核心逻辑复用**: 
  - 提取了通用的 `process_uploaded_file` 和 `get_file_content_summary` 逻辑，减少了重复代码。
  - 强化了对大文件解析及接口超时场景下的系统稳定性与异常拦截机制。
- **运行性能**: 逻辑解耦减小了主程序体积，提升了 Streamlit 平台的热加载与响应速度。

### 修复
- 解决了 4.8.1 版本中 MD 导出功能集成不完全的遗留问题。
- 修正了模型通道切换时可能导致工作区偏好设置意外丢失的逻辑缺陷。

---

## [4.8.1] - 2026-03-26
### 新增
- **Markdown 导出**: 引入 `export_utils` 模块，支持将完整对话导出为带附件关联及时间戳的 Markdown 资产。
- **工作区偏好记忆**: 每个工作区（Workspace）现在可以独立保存其模型通道、具体模型、System Prompt 以及所有生成参数（Temperature/Top P/Max Tokens）。

### 优化
- **联网意图可见性**: 优化了 SiliconFlow 模式下的联网意图判断展示，明确告知用户模型为何决定（或不决定）进行联网检索。
- **UI 细节**: 调整了附件显示的 Markdown 样式，并在添加新工作区时支持自动复制当前活动配置。

---

## [4.8.0] - 2026-03-25
### 新增
- **工作区管理 UI**: 在侧边栏新增“工作区管理”扩展面板，支持动态添加新工作区或删除非默认工作区。
- **智能联网意图判断**: 引入 SiliconFlow 通道的预置意图检测模型，自动判断是否需要调用 Tavily 进行联网搜索，避免无关对话浪费搜索配额。

### 优化
- **编码兼容性**: 增强了文本文件解析的健壮性，增加了对 GBK 等常见中文编码的自动 fallback 识别。
- **附件唯一性**: 优化了 Gem 知识库文件的命名逻辑，通过引入毫秒级时间戳及索引防止文件名冲突。

---

## [4.7.0] - 2026-03-23
### 新增
- **PDF 深度解析**: 集成 `PyMuPDF (fitz)` 库，支持对 PDF 附件进行高质量的文本提取（包括非标准编码 PDF 的初步识别）。
- **多附件并行支持**: 消息发送及 Gem 初始化现在支持同时上传和解析多个附件。
- **对话轮次删除**: 在消息操作区新增“删除此轮对话”功能，支持物理移除指定的 User + Assistant 对话对，并自动更新审计轨迹。

### 优化
- **布局防遮挡**: 通过 CSS 注入优化了 Streamlit 容器边距，解决了底部输入框与滚动条重叠的问题。

---

## [4.1.0] - 2026-03-18
### 修复
- **守护态逻辑增强**: 修复了页面刷新导致正在生成的 `pending_msg` 丢失的重大漏洞，现在所有中断的请求都会被安全转移到“草稿箱”。
- **Rerun 逻辑修复**: 解决了重新生成对话时可能产生的冗余时间戳注入问题，确保编辑态消息的清洁。

---

## [4.0.0] - 2026-03-10
### 新增
- **模型配置外置化**: 引入 `model_config.json`，支持在不修改源码的情况下动态更新支持的模型列表。
- **异常持久化渲染**: 实现了异常信息的跨 Rerun 显示，确保模型报错后用户能清楚看到错误原因。

### 优化
- **代码工程化**: 进行了大规模的代码格式化（Black Style）与结构重构，优化了 Gemini 历史记录的构建逻辑。

---

## [3.9.0] - 2026-03-08
### 新增
- **Gemini Cache 全生命周期管理**:
  - 支持在任意对话点手动建立上下文缓存。
  - 监控缓存 ID、TTL（生存时间）及过期倒计时。
  - 支持缓存续期与手动销毁。
- **底层 I/O 观察器**: 新增底层日志查看器，支持实时展开观察发往 API 的原始 JSON Payload 及返回的 Usage 元数据。
- **Token 预估**: 引入 `tiktoken` 配合官方 API，实现对当前上下文 Token 消耗的手动测算。
- **审计时间戳**: 自动为每条用户消息注入精确的提交时间戳（[提交于：YYYY-MM-DD HH:MM:SS]）。

---

## [3.8.0] - 2026-03-07
### 新增
- **工作区隔离架构 (Workspace)**: 实现了“默认”、“Gem”、“草稿”等多个隔离的工作区，每个区拥有独立的对话历史 JSON 文件。
- **Gem 专属定制**: 新增实例化流程，支持为特定任务注入长效固定指令与底层知识库文件。
- **初版守护态 (Guardian Mode)**: 首次引入 `pending_msg` 机制，防止由于 Streamlit UI 交互导致的生成中数据丢失。

---

## [3.5.0] - 2026-03-06
### 初始功能集
- **双通道架构**: 完美适配 Google GenAI SDK (Gemini) 与 OpenAI SDK (SiliconFlow)。
- **高级参数控制**: 支持 Temperature, Top P, Max Output Tokens 的手动调节。
- **用户反馈系统**: 支持对回答进行 👍/👎 反馈，并同步落盘。
- **重新生成功能**: 支持撤销上一轮并重新发起生成。
- **核心组件**: 联网检索（Tavily）、思维链（Thinking）剥离显示、JSON 资产导入导出、审计级流水日志。
